<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ט"ו באב בקיבוץ - הרשמה</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;600;800&family=Playfair+Display:ital,wght@1,600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Heebo', sans-serif;
            background-color: #fcf9f9;
        }
        .romantic-title {
            font-family: 'Playfair Display', serif;
        }
        .bg-pattern {
            background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23e0a8b9' fill-opacity='0.15'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
        }
    </style>
</head>
<body class="bg-pattern min-h-screen flex items-center justify-center p-4">

    <div class="max-w-2xl w-full bg-white rounded-3xl shadow-2xl overflow-hidden border border-rose-100">
        
        <!-- Header Section -->
        <div class="bg-gradient-to-r from-rose-400 to-pink-500 text-white p-8 text-center relative overflow-hidden">
            <!-- Decorative circles -->
            <div class="absolute -top-10 -right-10 w-32 h-32 bg-white opacity-10 rounded-full blur-2xl"></div>
            <div class="absolute -bottom-10 -left-10 w-32 h-32 bg-white opacity-10 rounded-full blur-2xl"></div>
            
            <h1 class="romantic-title text-4xl md:text-5xl font-bold mb-3 tracking-wide">ט"ו באב בקיבוץ</h1>
            <p class="text-xl md:text-2xl font-light opacity-90">חוגגים בטעם של פעם</p>
        </div>

        <!-- Content Section -->
        <div class="p-8 md:p-10">
            <div class="space-y-6 mb-10 text-gray-700">
                <div class="flex items-center gap-4 bg-rose-50 p-4 rounded-2xl">
                    <div class="w-12 h-12 bg-rose-200 text-rose-600 rounded-full flex items-center justify-center flex-shrink-0">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                    </div>
                    <div>
                        <p class="font-bold text-lg">יום שבת, 1 באוגוסט 2026</p>
                        <p class="text-sm text-gray-500">בשעה 20:00 (מוצאי שבת, י"ח באב)</p>
                    </div>
                </div>

                <div class="flex items-center gap-4 bg-rose-50 p-4 rounded-2xl">
                    <div class="w-12 h-12 bg-rose-200 text-rose-600 rounded-full flex items-center justify-center flex-shrink-0">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                    </div>
                    <div>
                        <p class="font-bold text-lg">מועדון לחבר</p>
                        <p class="text-sm text-gray-500">מחכים לכם שם!</p>
                    </div>
                </div>

                <div class="bg-pink-50 border-r-4 border-pink-400 p-5 rounded-l-2xl shadow-sm">
                    <p class="text-lg mb-2"><strong>בתכנית:</strong></p>
                    <ul class="list-disc list-inside space-y-2 text-gray-700">
                        <li>הופעה חיה של <strong>אייל שביט</strong> בשירי אהבה מכל הזמנים.</li>
                        <li>יין משובח ונשנושים מפנקים.</li>
                    </ul>
                </div>
            </div>

            <div class="border-t border-gray-100 pt-8">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">טופס הרשמה</h2>
                <p class="text-center text-rose-600 mb-6 text-sm">ההרשמה מראש חובה - אנא מלאו את הפרטים</p>
                
                <!-- 
                  Note: The 'action' attribute should be replaced with the Web App URL 
                  you get after deploying the Google Apps Script. 
                -->
                <form id="registrationForm" class="space-y-5">
                    
                    <div>
                        <label for="fullName" class="block text-sm font-medium text-gray-700 mb-1">שם מלא (או משפחה)</label>
                        <input type="text" id="fullName" name="fullName" required 
                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-rose-400 transition-colors shadow-sm bg-white"
                            placeholder="לדוגמה: משפחת כהן או ישראל ישראלי">
                    </div>

                    <div>
                        <label for="attendees" class="block text-sm font-medium text-gray-700 mb-1">מספר משתתפים</label>
                        <select id="attendees" name="attendees" required
                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-rose-400 transition-colors shadow-sm bg-white">
                            <option value="" disabled selected>בחרו מספר משתתפים</option>
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option value="3">3</option>
                            <option value="4">4</option>
                            <option value="5+">5 או יותר</option>
                        </select>
                    </div>

                    <div>
                        <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">טלפון ליצירת קשר</label>
                        <input type="tel" id="phone" name="phone" required pattern="[0-9]{9,10}"
                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-rose-400 focus:border-rose-400 transition-colors shadow-sm bg-white"
                            placeholder="לדוגמה: 0501234567">
                    </div>

                    <button type="submit" id="submitBtn"
                        class="w-full bg-gradient-to-r from-rose-500 to-pink-500 text-white font-bold py-4 rounded-xl hover:from-rose-600 hover:to-pink-600 focus:outline-none focus:ring-4 focus:ring-rose-300 transition-all transform hover:-translate-y-1 shadow-lg mt-4 flex justify-center items-center">
                        <span id="btnText">אישור הרשמה</span>
                        <!-- Loading Spinner (Hidden by default) -->
                        <svg id="loadingSpinner" class="animate-spin -ml-1 mr-3 h-5 w-5 text-white hidden" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                        </svg>
                    </button>

                    <!-- Success Message -->
                    <div id="successMessage" class="hidden mt-4 p-4 bg-green-50 text-green-700 rounded-xl border border-green-200 text-center">
                        <p class="font-bold">תודה רבה!</p>
                        <p>ההרשמה נקלטה בהצלחה, נתראה במועדון.</p>
                    </div>

                    <!-- Error Message -->
                    <div id="errorMessage" class="hidden mt-4 p-4 bg-red-50 text-red-700 rounded-xl border border-red-200 text-center">
                        <p>אופס... משהו השתבש.</p>
                        <p>אנא נסו שוב מאוחר יותר או פנו למארגנים.</p>
                    </div>
                </form>
            </div>
        </div>

        <!-- Admin Link -->
        <div class="bg-gray-50 py-3 text-center border-t border-rose-100">
            <button type="button" onclick="openAdminModal()" class="text-xs text-gray-400 hover:text-rose-500 transition-colors focus:outline-none">
                כניסת מנהלים
            </button>
        </div>
    </div>

    <!-- Admin Password Modal -->
    <div id="adminModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl p-6 max-w-sm w-full shadow-2xl relative">
            <button onclick="closeAdminModal()" class="absolute top-4 left-4 text-gray-400 hover:text-gray-700">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
            <h3 class="text-xl font-bold mb-4 text-gray-800 text-center">פאנל ניהול</h3>
            <p class="text-sm text-gray-600 mb-4 text-center">אנא הזן סיסמה כדי לצפות בנרשמים</p>
            <input type="password" id="adminPassword" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-rose-400 mb-4 text-center" placeholder="סיסמה">
            <p id="adminError" class="hidden text-red-500 text-sm mb-4 text-center">סיסמה שגויה</p>
            <button onclick="checkAdminPassword()" class="w-full bg-rose-500 text-white py-2 rounded-lg hover:bg-rose-600 transition-colors">כניסה</button>
        </div>
    </div>

        <!-- Admin Dashboard -->
    <div id="adminDashboard" class="hidden max-w-4xl w-full bg-white rounded-3xl shadow-2xl overflow-hidden border border-rose-100 flex flex-col max-h-[90vh]">
        <div class="bg-gray-800 text-white p-6 flex justify-between items-center">
            <h2 class="text-2xl font-bold">נרשמים לט"ו באב</h2>
            <button onclick="closeAdminDashboard()" class="bg-gray-700 hover:bg-gray-600 px-4 py-2 rounded-lg transition-colors text-sm">
                חזרה להרשמה
            </button>
        </div>
        <div class="p-6 overflow-y-auto flex-1 bg-gray-50">
            <div class="flex justify-between items-center mb-4">
                <div class="text-lg font-semibold text-gray-700">סה"כ משתתפים: <span id="totalAttendees" class="text-rose-600 font-bold">0</span></div>
                <button onclick="clearLocalData(this)" class="text-sm text-red-500 hover:text-red-700 underline transition-colors">נקה נתונים מקומיים</button>
            </div>
            <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-x-auto">
                <table class="w-full text-right border-collapse">
                    <thead class="bg-gray-100 text-gray-600 text-sm">
                        <tr>
                            <th class="p-3 border-b">שם מלא</th>
                            <th class="p-3 border-b">משתתפים</th>
                            <th class="p-3 border-b">טלפון</th>
                            <th class="p-3 border-b">תאריך הרשמה</th>
                        </tr>
                    </thead>
                    <tbody id="registrationsTableBody" class="text-gray-700 text-sm divide-y divide-gray-100">
                        <!-- Rows will be injected here -->
                    </tbody>
                </table>
                <div id="noDataMessage" class="hidden p-6 text-center text-gray-500">
                    אין עדיין נרשמים שמורים במערכת המקומית.
                </div>
            </div>
        </div>
    </div>

    <script>
        // REPLACE THIS URL with your published Google Apps Script Web App URL
        const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyCDRVvZPHyaACTEjAQ9NH7qpYEGpsjrZ_1W9CHOmyTXkeFVW6ywW9TcMmgN3U5yb0G/exec';

        const form = document.getElementById('registrationForm');
        const submitBtn = document.getElementById('submitBtn');
        const btnText = document.getElementById('btnText');
        const loadingSpinner = document.getElementById('loadingSpinner');
        const successMessage = document.getElementById('successMessage');
        const errorMessage = document.getElementById('errorMessage');

        // Admin Panel Elements
        const mainContainer = document.querySelector('.max-w-2xl');
        const adminModal = document.getElementById('adminModal');
        const adminDashboard = document.getElementById('adminDashboard');
        const adminPasswordInput = document.getElementById('adminPassword');
        const adminError = document.getElementById('adminError');
        const tableBody = document.getElementById('registrationsTableBody');
        const totalAttendeesSpan = document.getElementById('totalAttendees');
        const noDataMessage = document.getElementById('noDataMessage');

        form.addEventListener('submit', e => {
            e.preventDefault();
            
            // Save data locally for the admin panel dashboard
            const formData = new FormData(form);
            saveToLocalStorage(formData);

            // If the URL hasn't been set, just show a demo success message for the preview
            if (SCRIPT_URL === 'YOUR_GOOGLE_SCRIPT_WEB_APP_URL_HERE') {
                showLoading();
                setTimeout(() => {
                    showSuccess();
                    console.log("Demo mode: Form submitted. To connect to Google Sheets, replace SCRIPT_URL.");
                }, 1500);
                return;
            }

            // Real submission logic
            showLoading();
            
            fetch(SCRIPT_URL, {
                method: 'POST',
                body: formData
            })
            .then(response => {
                if(response.ok) {
                    showSuccess();
                } else {
                    showError();
                }
            })
            .catch(error => {
                console.error('Error!', error.message);
                showError();
            });
        });

        function showLoading() {
            submitBtn.disabled = true;
            submitBtn.classList.add('opacity-75', 'cursor-not-allowed');
            btnText.textContent = 'שולח...';
            loadingSpinner.classList.remove('hidden');
            successMessage.classList.add('hidden');
            errorMessage.classList.add('hidden');
        }

        function showSuccess() {
            submitBtn.classList.add('hidden');
            successMessage.classList.remove('hidden');
            form.reset();
        }

        function showError() {
            submitBtn.disabled = false;
            submitBtn.classList.remove('opacity-75', 'cursor-not-allowed');
            btnText.textContent = 'נסו שוב';
            loadingSpinner.classList.add('hidden');
            errorMessage.classList.remove('hidden');
        }

        // --- Admin Panel Logic ---

        function saveToLocalStorage(formData) {
            const data = {
                fullName: formData.get('fullName'),
                attendees: formData.get('attendees'),
                phone: formData.get('phone'),
                date: new Date().toLocaleString('he-IL')
            };
            
            let registrations = JSON.parse(localStorage.getItem('tuBavRegistrations') || '[]');
            registrations.push(data);
            localStorage.setItem('tuBavRegistrations', JSON.stringify(registrations));
        }

        function openAdminModal() {
            adminModal.classList.remove('hidden');
            adminPasswordInput.value = '';
            adminError.classList.add('hidden');
            adminPasswordInput.focus();
        }

        function closeAdminModal() {
            adminModal.classList.add('hidden');
        }

        function checkAdminPassword() {
            if (adminPasswordInput.value === '12345678') {
                closeAdminModal();
                showAdminDashboard();
            } else {
                adminError.classList.remove('hidden');
            }
        }

        // Allow pressing Enter to submit password
        adminPasswordInput.addEventListener('keypress', function (e) {
            if (e.key === 'Enter') {
                checkAdminPassword();
            }
        });

        function showAdminDashboard() {
            mainContainer.classList.add('hidden'); // Hide registration form
            adminDashboard.classList.remove('hidden'); // Show dashboard
            loadAdminData();
        }

        function closeAdminDashboard() {
            adminDashboard.classList.add('hidden');
            mainContainer.classList.remove('hidden');
        }

        function loadAdminData() {
            const registrations = JSON.parse(localStorage.getItem('tuBavRegistrations') || '[]');
            tableBody.innerHTML = '';
            
            let total = 0;

            if (registrations.length === 0) {
                noDataMessage.classList.remove('hidden');
                document.querySelector('table').classList.add('hidden');
            } else {
                noDataMessage.classList.add('hidden');
                document.querySelector('table').classList.remove('hidden');
                
                registrations.forEach(reg => {
                    // Extract number from string (e.g., "5+" becomes 5)
                    let count = parseInt(reg.attendees.replace('+', '')) || 0;
                    total += count;

                    const row = document.createElement('tr');
                    row.innerHTML = `
                        <td class="p-3 border-b">${reg.fullName}</td>
                        <td class="p-3 border-b">${reg.attendees}</td>
                        <td class="p-3 border-b" dir="ltr" style="text-align: right;">${reg.phone}</td>
                        <td class="p-3 border-b text-gray-500">${reg.date}</td>
                    `;
                    tableBody.appendChild(row);
                });
            }

            totalAttendeesSpan.textContent = total;
        }

        function clearLocalData(btn) {
            localStorage.removeItem('tuBavRegistrations');
            loadAdminData();
            
            // Temporary visual feedback
            const originalText = btn.textContent;
            btn.textContent = 'הנתונים נמחקו!';
            setTimeout(() => { btn.textContent = originalText; }, 2000);
        }
    </script>
</body>
</html>
